
#include "GUI.h"
/*
using namespace gui;

void gui::init() {}

const vec2& gui::AnchorPoint::operator*() const { return *pos; }
vec2& gui::AnchorPoint::operator*() { return *pos; }

void gui::AnchorPoint::check() {
	if (pos.isTriggered()) { update(); }
}

void gui::AnchorPoint::update() {
	// update self

	// update child
	for (auto& c : childs) { check(); }
}
*/